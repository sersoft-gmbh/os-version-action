name: OS version
description: Reads the current operating system (os) version
author: ser.soft GmbH
outputs:
  version:
    description: The current OS version (e.g. `24.04` for 'Ubuntu 24.04').
runs:
  using: node20
  main: dist/index.js
branding:
  color: gray-dark
  icon: info
